Build instructions
==================

Windows
-------

- Install Visual Studio Express 2013.
- Run vsvars32.bat (located in the VC installation).
- Install MSYS (we use VC to compile, but the build system needs a Unix-like environment).
- From MSYS prompt type "make".

Linux
-----

Type "make".


OSX
---

Type "make.
